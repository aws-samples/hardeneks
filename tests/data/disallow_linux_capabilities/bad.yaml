apiVersion: v1
kind: Pod
metadata:
  namespace: test-namespace
  name: bad
spec:
  containers:
  - name: nginx
    image: nginx
    securityContext:
      capabilities:
        add: ["NET_ADMIN", "SYS_TIME"]